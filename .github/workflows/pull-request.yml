name: "Pull Request"

on:
  pull_request:
    types: [ opened, synchronize ]

jobs:
  build-docker:
    uses: ./.github/workflows/build-docker.yml
    secrets: inherit
    with:
      push: false
      tag: ${{ github.sha }}
      target: integration
